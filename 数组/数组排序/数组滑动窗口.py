#源地址：https://algo.itcharge.cn/01_array/01_16_array_sliding_window/



"""
滑动窗口常用于查找满足某些条件的连续子区间，能将嵌套循环优化为单循环，大幅降低时间复杂度。
常见题型包括：
固定长度窗口：窗口大小固定，通常用于统计或查找长度为k的区间性质。
可变长度窗口：窗口大小不固定，常用于查找满足条件的最长/最短区间。

总结:
滑动窗口算法高效解决数组或字符串的连续区间问题。

固定长度窗口：窗口大小固定，常用于统计定长子区间的和、均值等。
不定长度窗口：窗口大小可变，适合查找最长/最短满足条件的子区间，如最长无重复子串。
滑动窗口通过动态调整左右边界，避免重复遍历，将时间复杂度从O(n^2) 降至O(n)。

使用时需注意：
窗口的起始位置
何时扩展或收缩窗口
如何及时更新答案
边界情况处理
"""


#代码实现
#固定长度滑动窗口模板
"""
left = 0  # 窗口左边界
right = 0  # 窗口右边界

while right < len(nums):
    # 将当前元素加入窗口
    window.append(nums[right])
    
    # 判断当前窗口长度是否达到 window_size
    if right - left + 1 >= window_size:
        # 在窗口长度达到要求时，进行答案的统计或更新
        # ... 这里根据题目需求维护/更新答案
        
        # 移除窗口最左侧元素，窗口向右滑动
        window.popleft()  
        left += 1  # 左指针右移，缩小窗口长度，保持窗口长度为 window_size
    
    # 右指针右移，扩大窗口
    right += 1
"""


#不定长度滑动窗口模板
"""
# 初始化左右指针，均指向数组起始位置
left = 0
right = 0

# 主循环，右指针遍历整个数组
while right < len(nums):
    # 将当前右指针指向的元素加入窗口
    window.append(nums[right])
    
    # 当窗口不满足题目要求时，缩小窗口（移动左指针）
    while 窗口需要缩小:
        # 此处可根据题意维护/更新答案
        window.popleft()  # 移除左边界元素
        left += 1         # 左指针右移，缩小窗口
    
    # 此处可根据题意维护/更新答案（如记录最大/最小窗口等）
    
    # 右指针右移，扩大窗口
    right += 1"""

