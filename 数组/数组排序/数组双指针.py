#源地址：https://algo.itcharge.cn/01_array/01_15_array_two_pointers/


"""

双指针总结:
双指针主要分为三类：「对撞指针」、「快慢指针」和「分离双指针」。

对撞指针：两个指针分别从序列两端向中间移动，常用于查找有序数组中满足特定条件的元素对、字符串反转等场景
对撞指针具体适用场景：
对撞指针常用于有序数组或字符串，典型应用包括：
查找有序数组中特定元素组合，如二分查找、两数之和等。
字符串或数组反转，如反转字符串、判断回文、颠倒二进制等。

快慢指针：两个指针从同一端出发，步长不同，常用于数组元素的移动、删除，或链表中的环检测、长度统计等问题。

分离双指针：两个指针分别遍历不同的数组或链表，适合处理有序数组的合并、交集、并集等问题。

双指针算法能够显著降低时间复杂度，通常可将暴力解法的 O(n^2) 优化为O(n)。其核心在于利用数据的有序性或问题的单调性，通过灵活移动指针，快速排除不符合条件的情况，从而减少无效计算。熟练掌握双指针技巧，可以高效解决大量数组和链表相关的问题。
"""


#代码实现
"""
三种指针的代码模板
对撞指针
# 初始化左右指针，分别指向数组的首尾
left, right = 0, len(nums) - 1

# 当左指针小于右指针时循环
while left < right:
    # 如果满足题目要求的特殊条件，直接返回结果
    if 满足要求的特殊条件:
        return 符合条件的值 
    # 如果满足某一条件，左指针右移，缩小区间
    elif 一定条件 1:
        left += 1
    # 如果满足另一条件，右指针左移，缩小区间
    elif 一定条件 2:
        right -= 1

# 如果循环结束还未找到，返回未找到或对应值
return 没找到 或 找到对应值



快慢指针
# 初始化慢指针 slow 和快指针 fast
slow = 0
fast = 1
# 当 fast 没有遍历到数组末尾时循环
while fast 未遍历到数组末尾：
    # 如果满足特定条件（如去重时 nums[fast] != nums[slow]）
    if 满足特定条件：
        slow += 1  # 慢指针右移一位，准备接收新元素
        # 根据实际需求，可能需要将 fast 指向的元素赋值给 slow 指向的位置
        # 例如：nums[slow] = nums[fast]
    fast += 1  # 快指针继续向右遍历
# 返回最终结果（如新数组长度 slow + 1 或处理后的数组等）
return 最终结果



分离指针
# 初始化两个指针，分别指向两个数组的起始位置
left_1, left_2 = 0, 0

# 当两个指针都未遍历到各自数组末尾时，循环进行比较
while left_1 < len(nums1) and left_2 < len(nums2):
    if 满足条件 1:
        # 通常表示两个指针指向的元素相等
        # 此时可以将该元素加入结果集（如交集），并同时移动两个指针
        left_1 += 1
        left_2 += 1
    elif 满足条件 2:
        # 通常表示第一个数组当前元素较小
        # 只移动第一个指针，继续比较下一个元素
        left_1 += 1
    elif 满足条件 3:
        # 通常表示第二个数组当前元素较小
        # 只移动第二个指针，继续比较下一个元素
        left_2 += 1
"""